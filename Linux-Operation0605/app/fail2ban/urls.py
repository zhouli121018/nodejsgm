# -*- coding: utf-8 -*-

from django.conf.urls import url
from . import views

urlpatterns = [
    # url(r'^settings$', views.fail2ban_settings, name='fail2ban_settings'),
    # url(r'^advance_setting$', views.fail2ban_advance_setting, name='fail2ban_advance_setting'),
    # url(r'^settings/banned_ips/$', views.fail2ban_banned_ips, name='fail2ban_banned_ips'),
    # url(r'^settings/ban_ip/$', views.fail2ban_ban_ip, name='fail2ban_ban_ip'),
    # url(r'^settings/(?P<name>\w+)/$', views.fail2ban_modify, name='fail2ban_modify'),
]